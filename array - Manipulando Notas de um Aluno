const notas = [5, 8, 9, 3, 10, 7]  
calcularMedia(notas)

function calcularMedia (notas)
{
    notas.sort((a, b) => b - a)
    let tres = notas.slice(0, 3)
    let soma = tres [0] + tres[1] + tres[2]
    let media = soma/3
    console.log(media)
    
}
